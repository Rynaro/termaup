#!/usr/bin/env bash
#
# bin/test — Run the test suite.
#
# Uses cargo if available, otherwise runs tests inside Docker.
#
# Usage:
#   bin/test
#   bin/test -- --test-threads=1
#
set -euo pipefail

cd "$(dirname "$0")/.."

exec bin/dev test "$@"
