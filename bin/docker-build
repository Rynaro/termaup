#!/usr/bin/env bash
#
# bin/docker-build — Build the Docker image.
#
# Usage:
#   bin/docker-build
#   bin/docker-build --no-cache
#
set -euo pipefail

cd "$(dirname "$0")/.."

exec docker compose build "$@"
